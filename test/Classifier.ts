import { expect } from "chai";
import { ethers } from "hardhat";

describe("Classifier", function () {
    it("Should grant the right owner the default admin role", async function () {
        //expect().to.equal();
    });
  
    it("Should grant the patient role to a specific address", async function () {
  
        // expect().to.equal();
    });

    it("Should fail to register if the user has already been registered", async function () {
  
        // expect().to.equal();
    });

    it("Should emit the granting of the right role to the right address", async function () {
  
        // expect().to.equal();
    });

})